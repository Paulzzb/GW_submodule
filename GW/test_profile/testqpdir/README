This is a test directory that contains
1. a 'test' input file
2. a 'demo' that, first runs 'test' into my database, then run qp_driver on the database.
3. The result (QPenergy/QPlog) is generated here.
