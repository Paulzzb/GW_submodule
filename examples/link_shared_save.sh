#!/usr/bin/env bash
# Obsolete for examples/: cases already share storage via
#   storage_dir = '../SAVE'
# Kept as a no-op so old docs/scripts do not fail hard.
echo "link_shared_save.sh: no-op (examples use shared cases/SAVE via namelist)."
exit 0
